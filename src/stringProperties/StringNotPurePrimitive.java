package stringProperties;

public class StringNotPurePrimitive {

	public static void main(String[] args) {
		
		//Any primitive variables has to be initialized as per their own type
		//Other primitive variables (int,double, char, Boolean etc.) have the only purpose to work
		//with their own typed variable declaration and initialization. 
		//Example of Apple watch
		//String declares variables
		//String can contain 
		//String creates objects
		//String has 17 constructors
		//String has many methods for value manupulation and modification
		
		//The Java documentation explains that a String isn't technically a primitive, 
		//but because it is often treated as one syntaxtically sometimes be called a primitive. 
	}

}
